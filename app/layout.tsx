import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'
import Header from '@/components/Header'
import Footer from '@/components/Footer'
import CosmicBadge from '@/components/CosmicBadge'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'Harley-Davidson | Motorcycles, Parts & Accessories',
  description: 'Discover the legendary Harley-Davidson motorcycles, genuine parts, accessories, apparel, and services. Find your local dealer and experience the freedom of the open road.',
  keywords: 'Harley-Davidson, motorcycles, touring, cruiser, parts, accessories, apparel, dealer',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  const bucketSlug = process.env.COSMIC_BUCKET_SLUG as string

  return (
    <html lang="en">
      <body className={inter.className}>
        <Header />
        <main>
          {children}
        </main>
        <Footer />
        <CosmicBadge bucketSlug={bucketSlug} />
      </body>
    </html>
  )
}